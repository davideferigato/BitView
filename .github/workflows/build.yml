name: Build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    
    - name: Print repository structure
      run: |
        echo "Repository structure:"
        ls -la
        echo "✅ Build completed successfully (no actual tests yet)."